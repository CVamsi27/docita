# API Deployment - Visual Guide & Workflows

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    Your Development                         │
│                   Make changes to API                       │
│                  Git commit & git push                      │
└────────────────┬──────────────────────────────────────────┘
                 │
                 ▼
        ┌────────────────────┐
        │  GitHub Repository │
        │   (main branch)    │
        └────────┬───────────┘
                 │
        ┌────────┴────────────────────────────────────┐
        │                                             │
        ▼                                             ▼
   ┌─────────────────────────┐          ┌──────────────────────┐
   │  GitHub Actions         │          │  Manual Deploy       │
   │  (Auto on push)         │          │  (SSH Build)         │
   │                         │          │                      │
   │ 1. Build image          │          │  Manual trigger      │
   │ 2. Push to Docker Hub   │          │  in Actions tab      │
   │ 3. SSH to EC2           │          │                      │
   └────────┬────────────────┘          └──────────┬───────────┘
            │                                      │
            │        Option A: Docker Hub          │ Option B: SSH Build
            │        (Recommended)                │
            │                                     │
            ▼                                     ▼
    ┌─────────────────────────────────────────────────┐
    │    AWS EC2 Instance (Ubuntu 22.04)              │
    │                                                 │
    │  1. SSH pull latest image                      │
    │  2. Stop existing container                    │
    │  3. Remove old container                       │
    │  4. Run new container with health checks       │
    │  5. Verify deployment success                  │
    └────────────┬────────────────────────────────────┘
                 │
                 ▼
        ┌────────────────────┐
        │    Nginx Reverse   │
        │     Proxy (80/443) │
        │                    │
        │ ✓ HTTP→HTTPS      │
        │ ✓ SSL/TLS         │
        │ ✓ WebSocket       │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │  API Container     │
        │ (Port 3001)        │
        │ NestJS Server      │
        └────────┬───────────┘
                 │
                 ▼
        ┌────────────────────┐
        │  Neon Database     │
        │ (External/Cloud)   │
        └────────────────────┘
```

---

## Deployment Flow - Option A: Docker Hub (Recommended)

```
Developer Push
    │
    ▼
┌─────────────────────────────────────┐
│  GitHub Actions Workflow            │
│  (deploy-api.yml)                   │
└─────────────────────────────────────┘
    │
    ├─ 1. Checkout code
    │
    ├─ 2. Setup Docker Buildx
    │       (faster builds)
    │
    ├─ 3. Login to Docker Hub
    │
    ├─ 4. Build Docker image
    │    (multi-stage build)
    │       ▼
    │    ┌──────────────────┐
    │    │ Build Stage      │
    │    │ (compile code)   │
    │    └──────────────────┘
    │       ▼
    │    ┌──────────────────┐
    │    │ Prod Stage       │
    │    │ (runtime only)   │
    │    └──────────────────┘
    │
    ├─ 5. Push to Docker Hub
    │    (latest + git SHA)
    │
    ├─ 6. SSH to EC2
    │
    ├─ 7. Pull latest image
    │
    ├─ 8. Stop old container
    │
    ├─ 9. Remove old container
    │
    ├─10. Run new container
    │    with health checks
    │
    ├─11. Wait for startup
    │
    ├─12. Verify container running
    │
    ├─13. Check health endpoint
    │
    ├─14. Send Slack notification
    │
    └─15. Done! ✅
```

**Time**: ~5-10 minutes per deployment

---

## Deployment Flow - Option B: SSH Build (Alternative)

```
Manual Trigger
    │
    ▼
┌─────────────────────────────────────┐
│  GitHub Actions Workflow            │
│  (deploy-api-ssh-build.yml)         │
│  Manual trigger                     │
└─────────────────────────────────────┘
    │
    ├─ 1. SSH to EC2
    │
    ├─ 2. Navigate to repo
    │    (~/docita-repo)
    │
    ├─ 3. Git fetch origin main
    │
    ├─ 4. Git reset --hard
    │
    ├─ 5. Build Docker image
    │    on EC2 directly
    │    (slower but simpler)
    │
    ├─ 6. Stop old container
    │
    ├─ 7. Remove old container
    │
    ├─ 8. Run new container
    │
    ├─ 9. Verify running
    │
    ├─10. Check health
    │
    ├─11. Cleanup old images
    │
    └─12. Done! ✅
```

**Time**: ~10-15 minutes per deployment

---

## EC2 Setup Steps Visual Flow

```
EC2 Instance Launched
    │
    ▼
┌──────────────────────────────┐
│ 1. Initial Setup             │
├──────────────────────────────┤
│ ✓ SSH into instance          │
│ ✓ Update packages            │
│ ✓ Install Docker             │
│ ✓ Install Nginx              │
│ ✓ Install Certbot            │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ 2. Allocate Elastic IP       │
├──────────────────────────────┤
│ ✓ Get Elastic IP from AWS    │
│ ✓ Associate with instance    │
│ ✓ Update DNS records         │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ 3. Create App Directory      │
├──────────────────────────────┤
│ ✓ mkdir ~/docita             │
│ ✓ Create .env file           │
│ ✓ Set permissions (600)      │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ 4. Configure Nginx           │
├──────────────────────────────┤
│ ✓ Remove default config      │
│ ✓ Create docita-api config   │
│ ✓ Enable site                │
│ ✓ Test config                │
│ ✓ Start Nginx                │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ 5. Setup SSL Certificate     │
├──────────────────────────────┤
│ ✓ Run certbot                │
│ ✓ Obtain certificate         │
│ ✓ Auto-renewal config        │
│ ✓ Update Nginx config        │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ 6. Configure GitHub Secrets  │
├──────────────────────────────┤
│ ✓ DOCKER_USERNAME            │
│ ✓ DOCKER_PASSWORD            │
│ ✓ EC2_HOST                   │
│ ✓ EC2_USERNAME               │
│ ✓ EC2_SSH_KEY                │
│ ✓ SLACK_WEBHOOK (optional)   │
└──────┬───────────────────────┘
       │
       ▼
      READY FOR DEPLOYMENT! 🚀
```

---

## Container Lifecycle

```
┌─────────────────────────────────────────┐
│         OLD CONTAINER RUNNING           │
│      (serving production traffic)        │
└────────────┬────────────────────────────┘
             │
        DEPLOYMENT STARTS
             │
             ▼
┌─────────────────────────────────────────┐
│      Pull new image from Docker Hub     │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│     STOP old container gracefully       │
│  (existing connections still active)    │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│    REMOVE old container completely      │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│    RUN new container                    │
│  (health checks enabled)                │
└────────────┬────────────────────────────┘
             │
      ┌──────┴──────┐
      │             │
      ▼             ▼
  WAITING       CHECKING
  40 SEC        HEALTH
      │             │
      └──────┬──────┘
             ▼
┌─────────────────────────────────────────┐
│     HEALTH CHECK PASSES ✅              │
│  Container ready to serve traffic       │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│    NEW CONTAINER SERVING TRAFFIC        │
│         (Deployment Complete)           │
└─────────────────────────────────────────┘
```

---

## GitHub Actions Dashboard View

```
┌─────────────────────────────────────────┐
│     GitHub Actions Workflow Run         │
├─────────────────────────────────────────┤
│  Deploy API to Production               │
│  Triggered: Push to main                │
│  Duration: ~8 minutes                   │
│                                         │
│  ✅ Checkout code              (10s)   │
│  ✅ Set up Docker Buildx       (5s)    │
│  ✅ Login to Docker Hub        (2s)    │
│  🔄 Build and Push Docker...   (4m)   │
│     └─ Building image...               │
│     └─ Pushing to Docker Hub...        │
│  🔄 Deploy to EC2 via SSH      (3m)   │
│     └─ Pulling Docker image...         │
│     └─ Stopping existing...            │
│     └─ Starting new container...       │
│     └─ Waiting for startup...          │
│     └─ Verifying container...          │
│     └─ Checking health...              │
│  ✅ Notify deployment status   (1s)    │
│                                         │
│  Result: ✅ SUCCESS                    │
└─────────────────────────────────────────┘
```

---

## File Structure After Update

```
docita/
├── .github/
│   └── workflows/
│       ├── deploy-api.yml ........................ NEW
│       ├── deploy-api-ssh-build.yml ............. NEW
│       ├── README.md ............................. NEW
│       └── test.yml
│
├── apps/
│   └── api/
│       ├── Dockerfile ............................ UPDATED
│       ├── src/
│       ├── dist/
│       └── ...
│
├── docs/
│   ├── DEPLOYMENT.md ............................ UPDATED
│   ├── DEPLOYMENT_API_SETUP.md .................. NEW
│   ├── DEPLOYMENT_API_QUICK_REF.md .............. NEW
│   ├── DEPLOYMENT_API_CHECKLIST.md .............. NEW
│   ├── DEPLOYMENT_API_CHANGES.md ................ NEW
│   ├── DEPLOYMENT_API_INDEX.md .................. NEW
│   ├── DEPLOYMENT_EXECUTIVE_SUMMARY.md ......... NEW
│   └── ...
│
└── ...
```

---

## Monitoring Dashboard Commands

```
REAL-TIME MONITORING
┌─────────────────────────────────────────┐
│ docker logs -f docita-api               │
├─────────────────────────────────────────┤
│ Shows live container logs               │
│ Useful for: troubleshooting, debugging  │
└─────────────────────────────────────────┘

CONTAINER STATUS
┌─────────────────────────────────────────┐
│ docker ps | grep docita-api             │
├─────────────────────────────────────────┤
│ Shows: running, ports, uptime           │
│ Useful for: quick health check          │
└─────────────────────────────────────────┘

DETAILED INFO
┌─────────────────────────────────────────┐
│ docker inspect docita-api               │
├─────────────────────────────────────────┤
│ Shows: config, mounts, environment      │
│ Useful for: debugging issues            │
└─────────────────────────────────────────┘

API HEALTH
┌─────────────────────────────────────────┐
│ curl https://api.docita.work/api/health │
├─────────────────────────────────────────┤
│ Should return: 200 OK with response     │
│ Useful for: verify API is working       │
└─────────────────────────────────────────┘

RESOURCE USAGE
┌─────────────────────────────────────────┐
│ docker stats docita-api                 │
├─────────────────────────────────────────┤
│ Shows: CPU, memory, network usage       │
│ Useful for: performance monitoring      │
└─────────────────────────────────────────┘

SYSTEM RESOURCES
┌─────────────────────────────────────────┐
│ df -h                                   │
├─────────────────────────────────────────┤
│ Shows: disk usage                       │
│ Useful for: verify space available      │
└─────────────────────────────────────────┘
```

---

## Troubleshooting Decision Tree

```
                   Issue Occurred
                        │
                        ▼
            ┌─────────────────────────┐
            │  Check GitHub Actions   │
            │  Workflow logs          │
            └────────┬─────────────────┘
                     │
          ┌──────────┴──────────┐
          │                     │
          ▼                     ▼
    GREEN ✅                RED ❌
    (Workflow               (Workflow
     passed)                 failed)
    │                         │
    ▼                         ▼
 Container              ┌──────────────┐
  problem?              │ Check error  │
    │                   │ message      │
    ▼                   └──────┬───────┘
┌──────────────┐              │
│  SSH to EC2  │              ▼
└────┬─────────┘        Common Issues:
     │                  - SSH key wrong
     ▼                  - Secrets not set
 Check logs:            - Network issue
 docker logs -f         - Out of space
 docita-api
     │
     ▼
 Fix issue &
 redeploy
```

---

## Success Indicators

### ✅ Deployment Success Signs

```
GitHub Actions:
├─ All workflow steps green ✅
├─ "✨ Deployment completed successfully!"
└─ No error messages

EC2 Container:
├─ docker ps shows docita-api running
├─ docker logs shows normal startup
└─ No restart loops

API Endpoint:
├─ curl returns 200 OK
├─ Response includes health data
└─ No connection errors

System Health:
├─ Nginx proxy working
├─ SSL certificate valid
└─ Logs show normal operation
```

### ❌ Deployment Failure Signs

```
GitHub Actions:
├─ Red X on workflow
├─ Error message in logs
└─ "Deployment failed"

EC2 Container:
├─ docker ps shows no container
├─ Container restarting repeatedly
└─ Error messages in docker logs

API Endpoint:
├─ Connection refused
├─ 502 Bad Gateway error
└─ curl times out

System Health:
├─ Nginx returning errors
├─ SSL certificate expired
└─ Out of disk space
```

---

## Quick Command Reference

### View Logs
```bash
docker logs -f docita-api           # Real-time logs
docker logs --tail 50 docita-api    # Last 50 lines
```

### Container Control
```bash
docker restart docita-api           # Restart container
docker stop docita-api              # Stop container
docker start docita-api             # Start container
docker ps                            # List running
```

### System Check
```bash
df -h                               # Disk usage
docker images | grep docita         # List images
docker system prune -a              # Clean up
```

### Health Check
```bash
curl https://api.docita.work/api/health
```

### SSH Connection
```bash
ssh -i your-key.pem ubuntu@your-ip
```

---

## Time Estimates

| Task | Time |
|------|------|
| EC2 Initial Setup | 10 minutes |
| Nginx & SSL Config | 15 minutes |
| Docker Build (first) | 5 minutes |
| Docker Build (subsequent) | 1 minute |
| Full Deployment | 5-10 minutes |
| First Full Setup | 1-2 hours |

---

**Total Implementation**: ✅ Complete
**Status**: 🚀 Production Ready
**Documentation**: 📚 Comprehensive

See `docs/DEPLOYMENT_EXECUTIVE_SUMMARY.md` for complete details.
